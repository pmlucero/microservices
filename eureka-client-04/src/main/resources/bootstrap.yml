spring:
  application:
    name: demo-eureka-adjective
  profiles:
    active: adjective    
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: secret
      fail-fast: true