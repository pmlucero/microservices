spring:
  application:
    name: demo-eureka-article
  profiles:
    active: article    
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: secret
      fail-fast: true