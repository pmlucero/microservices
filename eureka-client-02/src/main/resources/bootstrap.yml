spring:
  application:
    name: demo-eureka-verb
  profiles:
    active: verb  
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: secret
      fail-fast: true